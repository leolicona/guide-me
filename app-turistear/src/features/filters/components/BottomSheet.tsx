// Promoted to the shared primitive layer (src/components/BottomSheet). This re-export keeps
// existing `features/filters` imports working; prefer importing from '@/components' going forward.
export { BottomSheet } from '../../../components/BottomSheet'
